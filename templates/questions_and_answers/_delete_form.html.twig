<form method="post" action="{{ path('questions_and_answers_delete', {'id': questions_and_answer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ questions_and_answer.id) }}">
    <button class="btn">Delete</button>
</form>
